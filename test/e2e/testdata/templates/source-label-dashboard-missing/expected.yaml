description: >
  A Grafana dashboard with a metric missing source="deckhouse" selector must be
  flagged by the source-label rule.
module: module
expect:
  - linter: templates
    rule: source-label
    level: error
    textContains: "metric 'container_cpu_usage_seconds_total' in panel 'CPU Usage' must have source=\"deckhouse\" selector"
