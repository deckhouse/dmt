apiVersion: v1
kind: ConfigMap
metadata:
  name: e2e-yaml-injection-map
  namespace: e2e-yaml-injection
data:
{{- range $k, $v := .Values.e2eYamlInjection.injectedAnnotations }}
  # FLAGGED: unquoted map value -> a `\n---\n` value here would inject a document.
  {{ $k }}: {{ $v }}
{{- end }}
