apiVersion: v1
kind: ConfigMap
metadata:
  name: e2e-yaml-injection-scalar
  namespace: e2e-yaml-injection
data:
  # FLAGGED: unconstrained string rendered unquoted. The value's embedded `\n---\n`
  # breaks out of the scalar and injects the Secret document carried in its default.
  scalar: {{ .Values.e2eYamlInjection.injectedScalar }}
