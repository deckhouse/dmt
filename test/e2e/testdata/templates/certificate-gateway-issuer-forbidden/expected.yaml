description: >
  A Certificate that sets issuerRef.name via printf "letsencrypt-gateway-%s"
  must be flagged by the certificate-gateway-issuer rule.
module: module
expect:
  - linter: templates
    rule: certificate-gateway-issuer
    level: error
    textContains: "Certificates related to Gateway API must refer to issuer using"
