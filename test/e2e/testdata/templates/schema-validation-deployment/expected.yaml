description: >
  A rendered Deployment whose spec.replicas is a string instead of an integer
  must be flagged by the templates schema-validation rule, which decodes every
  standard resource into its Kubernetes Go type.
module: module
expect:
  - linter: templates
    rule: schema-validation
    level: error
    textContains: "does not match the Deployment API"
