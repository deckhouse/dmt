description: >
  A module that sets disable.messages with only one language (en, no ru) must be
  flagged as an error, because disable.messages must define both ru and en.
module: module
expect:
  - linter: module
    rule: definition-file
    level: error
    textContains: "must define both 'ru' and 'en'"
