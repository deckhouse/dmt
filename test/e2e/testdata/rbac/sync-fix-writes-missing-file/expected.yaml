description: >
  A capability file the declaration produces is missing and is reported by sync; --fix writes it
  from rbac.yaml and the finding is resolved in the same run.
kind: fix
module: module
expectPass:
  # the module must render: a render failure would otherwise pass every expectPass trivially
  - linter: manager
  - linter: rbac
    rule: sync
