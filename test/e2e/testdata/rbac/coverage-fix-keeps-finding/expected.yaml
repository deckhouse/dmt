description: >
  --fix appends an undecided stub (noAccess: "TODO") for the CRD without an entry, and the finding
  stays: a stub is not a decision, so the run that wrote it must not end green (spec 005 R33).
kind: fix
module: module
expect:
  - linter: rbac
    rule: coverage
    level: error
    textContains: "CRD e2e.deckhouse.io/globals (crds/vendor/globals.yaml) has no entry in rbac.yaml"
    count: 1
