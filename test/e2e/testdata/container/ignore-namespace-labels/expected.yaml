description: >
  A namespace with a name starting with "d8-" should be ignored by the
  container linter on the object-namespace-labels rule.
module: module
expectPass:
  - linter: container
    rule: object-namespace-labels
