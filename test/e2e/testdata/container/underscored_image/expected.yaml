description: >
  A deployment with an image name containing underscores should be flagged by the
  container linter on the image-no-underscores rule.
module: module
expect:
  - linter: container
    rule: image-no-underscores
    level: error
    textContains: 'image e2e_app contains underscores'